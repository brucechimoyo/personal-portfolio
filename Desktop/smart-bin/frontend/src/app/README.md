# App Directory

This is the main application folder. It contains the following subfolders:

- `core/`: Singleton services, interceptors, and app-wide guards.
- `shared/`: Shared components, pipes, directives, and modules.
- `features/`: Feature modules, each in its own subfolder.
- `layout/`: Layout components such as navigation, header, and footer. 